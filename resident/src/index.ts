/**
 * Host-side empty export: dsh.client dual-face packages must export
 * a host entry even when the host half is a no-op. The loader scans
 * this file for composition; the real work lives in src/client/.
 * @module @deepseek-ai/dsh-client-ui-pet
 */
export {}
